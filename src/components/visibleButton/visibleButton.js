import React from 'react';

import './visibleButton.scss';

const VisibleButton = () => {
  return (
    <button 
      className="button button--visible active" 
      type="button"> 
    </button>
  );
};

export default VisibleButton;