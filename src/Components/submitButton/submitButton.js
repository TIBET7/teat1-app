import React from 'react';

import './submitButton.scss';

const SubmitButton = () => {
  return(
    <button 
      className="submit-button" 
      type="submit">
      Применить 
    </button>
  );
};

export default SubmitButton;
