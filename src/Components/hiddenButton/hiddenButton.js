import React from 'react';

import './hiddenButton.scss';

const HiddenButton = () => {
  return (
    <button 
    className="button button--hidden active" 
    type="button"> 
  </button>
  );
};

export default HiddenButton;
