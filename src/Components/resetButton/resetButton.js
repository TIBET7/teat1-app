import React from 'react';

import './resetButton.scss';

const ResetButton = () => {
  return(
    <button 
      className="reset-button" 
      type="reset">
      Отменить 
    </button>
  );
};

export default ResetButton;
