import React from 'react';

import './addButton.scss';

function AddButton() {
  return (
    <button 
      className="button button--add" 
      type="button"
    >
    </button>
  );
};

export default AddButton;